// 
const A = getApp();
// 
const list = [];
// 
var playing = false;
// 
const PLAY = {
    // 
    play: function(url) {
        if (A.SYS.非正式测试) {
            // 
            A.ST.show('播放 : ' + url);
        }
    },
    //
};
module.exports = PLAY;